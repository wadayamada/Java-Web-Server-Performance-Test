plugins {
    id 'java'
    id 'application'
}

application {
    mainClass = 'Server'
    applicationDefaultJvmArgs = ['-Xlog:gc*', '-XX:+UseG1GC']
}

repositories { mavenCentral() }

dependencies {
    implementation 'io.netty:netty-all:4.1.95.Final'
}

tasks.withType(JavaCompile) { options.encoding = 'UTF-8' }
