plugins {
    id 'java'
    id 'application'
}

application {
    mainClass = 'Server'
    applicationDefaultJvmArgs = ['-Xlog:gc*:file=gc.log:time']
}

repositories { mavenCentral() }

dependencies {
    implementation 'io.netty:netty-all:4.1.95.Final'
}

tasks.withType(JavaCompile) { options.encoding = 'UTF-8' }
